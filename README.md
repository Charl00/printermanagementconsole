# A Printer Management Console

This is a simple printer management console created using NextJS and Firebase.

## Getting Started

run `yarn` or `npm i` to install packages.

run `yarn dev` to run the application.
